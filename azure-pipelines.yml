trigger:
- main

variables:
  azureServiceConnectionId: 'b2b461d6-4204-4ad7-a084-ed4f1f2bda5d'
  webAppName: 'harshitwebapp'
  pythonVersion: '3.11'

stages:

- stage: Build
  displayName: Build Stage
  jobs:
  - job: BuildJob
    pool:
      name: TestAgent   # ✅ Self-hosted pool

    steps:
    - script: |
        echo Python Version
        python --version
      displayName: Check Python

    - script: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
      displayName: Install Dependencies

    - task: ArchiveFiles@2
      inputs:
        rootFolderOrFile: '.'
        includeRootFolder: false
        archiveType: zip
        archiveFile: $(Build.ArtifactStagingDirectory)/app.zip
        replaceExistingArchive: true
      displayName: Archive Files

    - publish: $(Build.ArtifactStagingDirectory)/app.zip
      artifact: drop

- stage: Deploy
  displayName: Deploy Stage
  dependsOn: Build
  condition: succeeded()

  jobs:
  - deployment: DeploymentJob
    pool:
      name: TestAgent   # ✅ Self-hosted pool again

    environment: harshitwebapp

    strategy:
      runOnce:
        deploy:
          steps:
          - task: AzureWebApp@1
            inputs:
              azureSubscription: $(azureServiceConnectionId)
              appType: webAppLinux
              appName: $(webAppName)
              package: $(Pipeline.Workspace)/drop/app.zip
